services:
  - type: web
    name: the-barber
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: the-barber-db
          property: connectionString

databases:
  - name: the-barber-db
    databaseName: barbearia
    plan: free
